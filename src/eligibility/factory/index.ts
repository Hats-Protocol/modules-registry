import * as info from "./info.json";
import { abi } from "./abi";
//import type { EligibilityFactory } from "eligibility/types";

const factory = {
  factoryInfo: info,
  factoryAbi: abi,
};

export { factory };
